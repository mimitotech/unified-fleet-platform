---
# cPanel Git Version Control — also paste scripts/cpanel-deploy.sh into
# the panel "Deployment Script" field (that field is what Deploy often runs).
deployment:
  tasks:
    - /bin/bash scripts/cpanel-deploy.sh
