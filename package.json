{
  "name": "mams",
  "private": true,
  "description": "Hostinger Root directory must be: platform — Entry: hostinger-start.mjs",
  "scripts": {
    "dev": "npm run dev --prefix platform",
    "build": "npm run build --prefix platform",
    "build:hostinger": "npm run build:hostinger --prefix platform",
    "start": "npm start --prefix platform",
    "setup": "npm run setup --prefix platform"
  },
  "engines": {
    "node": ">=22"
  }
}
