# Legacy Node application

Archived reference for finishing the PHP port (Wialon, fuel, workshop, etc.).

**Not served by Apache** (blocked in `.htaccess`).

Do not point the domain document root here. Use the repo root PHP app instead.
